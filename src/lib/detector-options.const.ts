// Interface.
import { DetectorOptions } from '../interface/detector-options.interface';

/**
 * 
 */
export const DETECTOR_OPTIONS: DetectorOptions = {
  changeDetector: 'changeDetector',
  detached: 'detached',
  detection: 'detection',
  properties: 'properties',
};
